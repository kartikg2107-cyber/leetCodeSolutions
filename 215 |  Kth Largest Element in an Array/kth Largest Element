class Solution {
    public int findKthLargest(int[] nums, int k) {
        Arrays.sort(nums);
        int count=0;
        for(int i=nums.length-1;i>nums.length-1-k;i--){
          count=nums[i];
        }
        return count;
    }
}
